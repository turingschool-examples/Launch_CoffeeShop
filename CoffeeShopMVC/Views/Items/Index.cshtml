@model IEnumerable<CoffeeShopMVC.Model.Item>
<h1>Items</h1>

@if (Model.Any() != false)
{
    @foreach (var item in Model)
    {
        <div>
            <p>Name: @item.Name, Price: @((@item.PriceInCents / 100m).ToString("C2"))</p>
        </div>
    }
}
else
{
    <p>No data available.</p>
}

